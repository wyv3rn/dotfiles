#!/usr/bin/env bash

if [ "$(uname)" == "Linux" ]; then
    bin=rofi
    args="-dmenu -matching fuzzy"
else
    bin=choose
    args=-az
fi

if [ $# -eq 1 ]; then
    echo -e "$1" > /Users/dschatz/gg.txt
    echo -e "$1" | $bin $args
else
    $bin $args
fi
